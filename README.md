# Транскрибатор v0.18

Локальный инструмент транскрибации видео и аудио для команды. Веб-интерфейс с тремя вкладками: транскрибация, история, анализ. Работает без интернета после первой загрузки модели.

**Движки:**
- **Apple Silicon (M1/M2/M3)** — mlx-whisper (быстро, нативно)
- **Windows / Linux / Intel Mac** — faster-whisper (CPU, int8)

---

## Возможности

### Транскрибация
- Перетащи файл или выбери через диалог
- Выбор модели (tiny → large) и языка (RU / EN / Авто)
- Стриминг сегментов в реальном времени по мере распознавания
- Кнопка отмены, индикатор скорости (X× real-time)
- Аудиоплеер: воспроизведение, перемотка кликом по таймкоду, подсветка активного сегмента
- Редактирование текста сегментов прямо в интерфейсе
- Экспорт: **TXT**, **SRT**, **Аудио**

### История (вкладка «История»)
- Каждая транскрибация сохраняется автоматически
- Список с датой, именем файла, длительностью
- Скачивание TXT / SRT / аудиодорожки из любой записи
- Удаление записи

### Анализ (вкладка «Анализ»)
- Суммаризация транскрипта с помощью Claude
- Источник: текущая сессия или любая запись из истории
- Настройки: число участников, тип созвона, цель, режим
- Поддержка своего промпта (полностью заменяет стандартный)
- Результат — HTML-документ, скачивается автоматически
- История анализов сохраняется локально (до 20 записей)
- Требует ключ доступа

---

## Changelog — last update 17.04.2026

### v0.19

- Анализ созвонов теперь работает без VPN — туннель через ngrok
- ffmpeg поставляется внутри пакета (`imageio-ffmpeg`), больше не нужен Homebrew на Mac

### v0.18

- Новая вкладка **История**: список транскрибаций с загрузкой TXT / SRT / Аудио
- Новая вкладка **Анализ**: суммаризация через Claude, выбор источника из истории или текущей сессии
- Форма анализа: степпер −/+ для числа участников, 4 типа созвона, 7 целей
- Кнопка **Скачать аудио** в разделе экспорта
- Статус-бейдж перенесён из хедера в правую панель
- Рефакторинг: `index.html` разбит на HTML + `style.css` + `script.js`
- Фикс мультиселекта в кнопках выбора опций анализа

### v0.17

- Редактирование текста сегментов прямо в интерфейсе
- Светлая тема (кнопка в левом нижнем углу)
- Автоустановка Homebrew и ffmpeg на Mac при первом запуске
- Фикс: кнопка «Отменяю...» больше не зависает при отмене

### v0.16

- Аудиоплеер: воспроизведение, перемотка кликом по таймкоду, подсветка активного сегмента
- Автообновление из GitHub при каждом запуске

---

## Установка

### Mac

**Шаг 1.** Открой Терминал (`Cmd+Пробел` → "Терминал")

**Шаг 2.** Клонируй репозиторий:
```bash
git clone https://github.com/des-e/transcribe.git ~/transcribe
```

> Если git не установлен — появится окно с предложением установить Xcode Command Line Tools. Нажми «Установить» и дождись окончания.

**Шаг 3.** Запусти:
```bash
bash ~/transcribe/start.sh
```

При первом запуске автоматически установятся:
- **uv** — менеджер Python-окружений
- **Homebrew** — если не установлен (потребуется пароль администратора)
- **ffmpeg** — для видеофайлов
- **Модель Whisper medium** (~1.5 GB, скачивается один раз)

**Шаг 4.** Открой браузер: [http://localhost:8000](http://localhost:8000)

---

**Опционально: иконка в Dock**

Чтобы запускать одним кликом из Dock:
```bash
bash ~/transcribe/make_app.sh
```
Создаст `Transcribe.app` на Рабочем столе — перетащи его в Dock.

---

### Windows

**Шаг 1.** Установи [Git для Windows](https://git-scm.com/download/win), если не установлен

**Шаг 2.** Открой PowerShell и клонируй репозиторий:
```powershell
git clone https://github.com/des-e/transcribe.git $env:USERPROFILE\transcribe
```

**Шаг 3.** Запусти `start.bat` двойным кликом

При первом запуске автоматически установится uv и скачается модель.

> ffmpeg нужен для видеофайлов. Установи отдельно:
> ```
> winget install Gyan.FFmpeg
> ```
> После установки перезапусти `start.bat`.

**Шаг 4.** Открой браузер: [http://localhost:8000](http://localhost:8000)

---

## Обновление

Обновления применяются **автоматически** при каждом запуске — скрипт проверяет GitHub и обновляет файлы.

Если нужно обновить вручную:
```bash
cd ~/transcribe && git pull
```

---

## Модели

| Модель | Качество | Скорость |
|--------|----------|----------|
| `tiny`   | низкое   | очень быстро |
| `base`   | приемлемо | быстро |
| `small`  | хорошее  | сбалансировано |
| `medium` | отличное | рекомендуется ← |
| `large`  | максимум | медленно |

Первый запуск выбранной модели скачает и закеширует её. Последующие запуски — мгновенные.

---

## Форматы файлов

**Видео** (требует ffmpeg): MP4, MKV, AVI, MOV, WEBM  
**Аудио**: MP3, M4A, WAV, OGG, AAC, FLAC
